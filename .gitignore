# Training artifacts and large binaries (kept on the cluster / W&B; not in git).
# Comments must be on their own line — git does not support trailing comments.

# trained .pt weights — too large for git
phase1_checkpoints/
phase2_checkpoints/

# empty stderr stubs + local W&B run cache
logs/

# local W&B run cache
wandb/

# raw + processed experiment data IS tracked (see results/)

# paper drafts, LaTeX source and materials — kept locally, not in the code repo
paperInfo/

# one-off analysis/figure-generation scripts — kept locally for reference, outside
# the tracked folders so the repo and the Amathuba zip submission match; the raw
# data (games.csv, results/) and the paper's written methodology are the record of truth
/analyze_phase2.py
paperMaterials/

# scratch personal notes — kept locally, not part of the submission
/notes.txt

# python + macOS cruft
__pycache__/
*.pyc
.DS_Store
