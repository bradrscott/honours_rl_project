% ── Table of experiments (Section 3.3) ── requires \usepackage{booktabs}
% Full experimental protocol, Phase 1 (capability) vs Phase 2 (shift & recovery).
% Uses table* to span both columns in the two-column layout.
\begin{table*}[t]
  \centering
  % Add your own \caption{...} here.
  \label{tab:experiments}
  \small
  \setlength{\tabcolsep}{7pt}
  \begin{tabular}{@{}p{2.1cm} p{6.4cm} p{7.1cm}@{}}
    \toprule
     & \textbf{Phase 1 — Capability} & \textbf{Phase 2 — Shift \& Recovery} \\
    \midrule
    Purpose
      & Train each agent to a stable per-opponent win-rate baseline and save a checkpoint.
      & Measure recovery after abrupt opponent strategy shifts --- the research question. \\
    \addlinespace[3pt]
    Agents      & PPO, FuN & PPO, FuN \\
    Board sizes & $9{\times}9$,\ \ $13{\times}13$ & $9{\times}9$,\ \ $13{\times}13$ \\
    Opponents
      & 5 fixed heuristic bots: greedy, defensive, corner, edge, random.
      & Shifts between four of them, by magnitude: LOW corner$\to$edge, MED corner$\to$defensive, HIGH greedy$\to$defensive. \\
    Shift frequency & --- & f1 (single), f2 (periodic), f3 (frequent) \\
    Steps per run   & 3M ($9{\times}9$),\ \ 5M ($13{\times}13$) & 2.3M ($9{\times}9$),\ \ 4.0M ($13{\times}13$) \\
    Seeds           & 1 & 3\ \ (0,\ 1,\ 2) \\
    \addlinespace[3pt]
    Runs
      & $2 \times 5 \times 2 = 20$
      & $3 \times 3 \times 2 \times 2 \times 3 = 108$ \\
    Recorded
      & rolling win rate (the baseline)
      & recovery time, dip depth, adaptation cost, policy reuse \\
    \midrule
    \multicolumn{3}{@{}p{15.6cm}}{\textbf{Total: 128 training runs}, plus a GNU~Go reference of
      100 games vs each opponent (Table~\ref{tab:gnugo}).}\\
    \bottomrule
  \end{tabular}
\end{table*}
